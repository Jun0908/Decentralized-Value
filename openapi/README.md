# OpenAPI

The versioned Frontier HTTP API specification will be added in Phase 5. Bazantic requires a publicly reachable HTTPS specification with at least one operation; its CLI does not accept a local-only spec URL.
