# OpenAPI

[`frontier-v1.yaml`](frontier-v1.yaml) is the versioned public contract used by the web app,
external agents, and the Bazantic service. Read endpoints are free. `evaluateArtifact` is the
single paid/gated operation when the API is accessed through Bazantic's 402/MPP gateway.
